services:
  postgres:
    image: postgres:latest
    container_name: question_management
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
      POSTGRES_DB: question_management
    ports:
      - "5432:5432"
    volumes:
      - ./data:/var/lib/postgresql/data
volumes:
  postgres-data:
    driver: local
